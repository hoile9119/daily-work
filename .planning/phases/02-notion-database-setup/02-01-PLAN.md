---
phase: 02-notion-database-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/state.json
autonomous: false
requirements: [NDB-01, NDB-02, NDB-03, NDB-04]

must_haves:
  truths:
    - "Projects Notion database exists with Name (title) and Description (rich_text) properties"
    - "Tasks Notion database exists with Name, Status (select: new/in progress/done/pending), Summary, Raw Notes, Date, and Project (relation) properties"
    - "Both database IDs are persisted in .planning/state.json under keys projectsDbId and tasksDbId"
    - "A test Task page linked to a test Project page shows a non-empty Project relation (UUID-based, not name-based)"
    - "Re-executing this plan when state.json already has both IDs skips creation and exits cleanly"
  artifacts:
    - path: ".planning/state.json"
      provides: "Cached Notion database UUIDs for Phase 3 consumption"
      contains: "projectsDbId, tasksDbId"
  key_links:
    - from: "Tasks DB 'Project' relation property"
      to: "Projects DB data_source_id"
      via: "relation.data_source_id captured from Projects create-database response"
      pattern: "response.data_sources[0].id"
---

<objective>
Create two Notion databases (Projects and Tasks), wire a UUID-based relation between them,
persist both database IDs to .planning/state.json, and validate the relation by creating and
trashing test pages.

Purpose: Phase 3 (sync pipeline) requires both databases to exist with correct schemas and
their IDs cached locally. This phase establishes that foundation.

Output: .planning/state.json with { projectsDbId, tasksDbId }; two Notion databases live
in the user's chosen workspace location.

**CRITICAL ARCHITECTURAL NOTE:**
Claude IS the executor. There are no Node.js scripts to run. Every step below is a
description of MCP tool calls Claude must make, bash file-read/write operations Claude
must perform, or checkpoints requiring user input. When a step says "call notion-notion-X",
Claude calls that MCP tool directly.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-notion-database-setup/CONTEXT.md
@.planning/phases/02-notion-database-setup/RESEARCH.md
@src/normalizeStatus.mjs

<!-- Key constraints carried forward from research:
  - Tool names: notion-notion-create-database, notion-notion-fetch,
                notion-notion-create-pages, notion-notion-update-page
  - Status select options MUST exactly match: "new", "in progress", "done", "pending"
  - DB ID cache keys: projectsDbId, tasksDbId
  - Relation uses data_source_id (NOT database_id) from Projects DB create response
  - dual_property relation: Tasks gets "Project"; Projects auto-gets "Tasks" back-link
  - state.json is gitignored (set in Phase 1); do NOT commit it
-->
</context>

## ⚠️ Read Before Executing

This plan uses **Notion MCP tool calls** — Claude is the executor. No JavaScript files are
created. Each task describes what Claude must DO (MCP calls, file reads/writes), not code
to write.

**Assumption A1 (from RESEARCH.md):** `notion-notion-create-database` may accept either
(a) a structured JSON properties object matching the Notion REST API, or (b) a different
format. Task 0.3 resolves this before any DB is created.

**Assumption A2:** `create-database` response may or may not include `data_sources[0].id`.
Task 1.2 includes a fallback: if absent, call `notion-notion-fetch` on the Projects DB ID.

<tasks>

<!-- ═══════════════════════════════════════════════
     WAVE 0 — PRE-FLIGHT: IDEMPOTENCY + TOOL INSPECTION
     ═══════════════════════════════════════════════ -->

<task type="auto" id="0.1">
  <name>Task 0.1: Idempotency Check — Read state.json</name>
  <files>.planning/state.json (read-only)</files>

  <action>
Read the file `.planning/state.json` using bash:

```bash
cat .planning/state.json 2>/dev/null || echo "{}"
```

**Decision tree:**

**Case A — Both IDs present:**
```json
{ "projectsDbId": "some-uuid", "tasksDbId": "some-uuid" }
```
→ Proceed to **Task 0.2 (verify DBs are reachable)**. Do NOT re-create.

**Case B — File missing or one/both IDs absent:**
→ Proceed to **Task 0.4 (parent page location checkpoint)**. Will create DBs.

**Case C — File exists but is malformed JSON:**
→ Treat as Case B. Note the malformed content for the summary.
  </action>

  <verify>
    Output from `cat .planning/state.json` is inspected. Route to 0.2 or 0.4 based on
    presence/absence of projectsDbId and tasksDbId keys.
  </verify>
  <done>State cache check complete — routing decision made. No user action required.</done>
</task>

<task type="auto" id="0.2">
  <name>Task 0.2: Verify Existing DBs Are Reachable (only if state.json has both IDs)</name>
  <files>.planning/state.json (read-only)</files>

  <action>
**Only execute this task if Task 0.1 found both projectsDbId and tasksDbId.**

Call `notion-notion-fetch` twice — once per database ID:

**Call A — Projects DB:**
```
notion-notion-fetch
  id: <projectsDbId from state.json>
```

**Call B — Tasks DB:**
```
notion-notion-fetch
  id: <tasksDbId from state.json>
```

**Decision tree:**

**Both return valid database objects (no error, no 404):**
→ Log: "Both Notion databases exist and are reachable. Skipping creation."
→ **Jump directly to Task 2.1 (relation validation)**. Skip Tasks 0.3 through 1.4.

**Either returns 404 or error:**
→ Log which DB is missing (Projects, Tasks, or both).
→ **Proceed to Task 0.4 (parent page checkpoint)**. Will re-create missing DBs.
→ Note: if only one DB is missing, still re-create BOTH (to ensure schemas are consistent
  and the relation data_source_id is fresh).
  </action>

  <verify>
    notion-notion-fetch returns an object with `object: "database"` for each ID,
    OR returns an error indicating the DB doesn't exist.
  </verify>
  <done>Reachability determined. Either skip to validation, or proceed to creation.</done>
</task>

<task type="auto" id="0.3">
  <name>Task 0.3: Inspect notion-notion-create-database Tool Schema</name>
  <files>(none — read-only tool schema inspection)</files>
**Only execute if proceeding to DB creation (Case B from 0.1 or stale IDs from 0.2).**

Before calling `notion-notion-create-database`, inspect its input schema to determine
what parameter format it expects. Do this by examining the tool description in the MCP
tool list.

Look for:
1. **Parameter names** — Does it have a `properties` field? A `schema` field? A `title` field?
2. **Format of properties** — JSON object? SQL DDL string? Natural language?

**Expected parameter shape (JSON — most likely based on RESEARCH.md):**
```json
{
  "parent":     { "type": "page_id", "page_id": "<UUID>" },
  "title":      [{ "type": "text", "text": { "content": "Projects" } }],
  "properties": { ... }
}
```

**Alternative DDL string shape (if tool description mentions SQL syntax):**
```
Name TITLE, Description RICH_TEXT
```

**Decision:**
- If JSON properties object → use the JSON schemas in Tasks 1.1 and 1.3 as-is.
- If DDL string → use the DDL strings defined in Tasks 1.1 and 1.3 as-is.
- If unclear → use JSON (it matches the underlying Notion REST API).

Record the confirmed format in your working memory for Tasks 1.1 and 1.3.
  </action>

  <verify>Tool schema inspected. Format (JSON or DDL) confirmed.</verify>
  <done>
    Confirmed parameter format for notion-notion-create-database. Proceeding to DB creation
    with the correct format.
  </done>
</task>

<task type="checkpoint:human-verify" id="0.4">
  <name>Task 0.4: Confirm Parent Page Location</name>

  <action>
**Only execute if proceeding to DB creation.**

Ask the user:

> **Where in your Notion workspace should I create the Projects and Tasks databases?**
>
> Please provide one of:
> 1. **A Notion page URL** — e.g., `https://www.notion.so/My-Workspace-Page-abc123...`
>    → I will create both databases under that page.
> 2. **"workspace root"** — I will create a parent page called "Daily Work Tracker" at the
>    top level, then place both databases inside it.
>
> *Note: "workspace root" requires your Notion integration to have workspace-level access.
> If you get a permission error, provide a specific page URL instead.*

**UUID extraction from a Notion URL:**
If the user provides a URL like `https://www.notion.so/My-Page-3a4b5c6d7e8f...`:
- The UUID is the last 32 hex characters: `3a4b5c6d7e8f...`
- Format: `3a4b5c6d-7e8f-xxxx-xxxx-xxxxxxxxxxxx`
- Store as PARENT_PAGE_ID for Tasks 1.1 and 1.3.

**If "workspace root":**
- Store PARENT_TYPE = "workspace" (no page_id needed).
- The parent object in the create-database call will be: `{ "type": "workspace" }`
  (some Notion API versions use this; if it fails, create a parent page first).
  </action>

  <what-built>Nothing yet. This captures the required parent location before any DB creation.</what-built>
  <how-to-verify>
    User has provided either a Notion page URL or confirmed "workspace root".
    Claude has extracted and stored PARENT_PAGE_ID (or noted workspace root).
  </how-to-verify>
  <resume-signal>
    User replies with a Notion page URL or the word "workspace root".
    Claude extracts UUID if needed and proceeds to Task 1.1.
  </resume-signal>
</task>


<!-- ═══════════════════════════════════════════════
     WAVE 1 — CREATE DATABASES + WRITE STATE
     ═══════════════════════════════════════════════ -->

<task type="auto" id="1.1">
  <name>Task 1.1: Create Projects Notion Database</name>
  <files>.planning/state.json (written in Task 1.4)</files>

  <action>
Call `notion-notion-create-database` to create the Projects database.

**If tool takes JSON (most likely):**
```json
{
  "parent": {
    "type": "page_id",
    "page_id": "<PARENT_PAGE_ID from Task 0.4>"
  },
  "title": [
    { "type": "text", "text": { "content": "Projects" } }
  ],
  "properties": {
    "Name":        { "title": {} },
    "Description": { "rich_text": {} }
  }
}
```

**If parent is workspace root (user chose that in 0.4):**
Replace the `parent` block with:
```json
"parent": { "type": "workspace" }
```
If that fails with an error, create a parent page first:
```
notion-notion-create-pages
  parent: null (or workspace)
  properties: { "title": [{ "type": "text", "text": { "content": "Daily Work Tracker" } }] }
```
→ Capture the returned page ID as PARENT_PAGE_ID, then retry Task 1.1 with that ID.

**If tool takes DDL string:**
Pass the schema as:
```
Name TITLE, Description RICH_TEXT
```
With the database title "Projects" in whatever title parameter the tool exposes.

**After the call:**
From the response, capture:
- `response.id` → store as **PROJECTS_DB_ID** (the database UUID, used for state.json and fetching pages)
- `response.data_sources[0].id` → store as **PROJECTS_DS_ID** (the data_source_id, used for relation wiring)

If `data_sources` is not in the response, proceed to Task 1.2 to retrieve PROJECTS_DS_ID separately.

**On failure (400, 403, etc.):**
- 403: Notion integration lacks access to the parent page → ask user to share the page
  with the integration in Notion settings → retry.
- 400: Schema format wrong → try the DDL format if you used JSON, or vice versa → retry.
- Any other error: Log full error and stop. Do not proceed to Task 1.3.
  </action>

  <verify>
    notion-notion-create-database returns an object with `object: "database"` and an `id` field.
    PROJECTS_DB_ID is captured and non-empty.
  </verify>
  <done>
    Projects DB created in Notion. PROJECTS_DB_ID captured. PROJECTS_DS_ID captured or
    flagged for retrieval in Task 1.2.
  </done>
</task>

<task type="auto" id="1.2">
  <name>Task 1.2: Capture Projects data_source_id (for relation wiring)</name>
  <files>(no files)</files>

  <action>
**Skip this task if PROJECTS_DS_ID was already captured from Task 1.1's response.**

If `data_sources` was absent from the create-database response, call:
```
notion-notion-fetch
  id: <PROJECTS_DB_ID>
```

From the fetch response, look for one of these paths to the data source UUID:
1. `response.data_sources[0].id` ← preferred
2. `response.id` ← fallback (if the API uses database_id directly for relations)

Store the result as **PROJECTS_DS_ID**.

**Why this matters:** The Tasks DB `Project` relation property requires `data_source_id`,
NOT `database_id`. These are different UUIDs in the Notion API (v2025-09-03). Using the
wrong one will cause the relation to fail silently or return a 400.

**If neither path yields a UUID:**
Use PROJECTS_DB_ID as PROJECTS_DS_ID (fallback — some older API versions accept database_id
in the relation field). Note this assumption in the summary.
  </action>

  <verify>PROJECTS_DS_ID is a non-empty UUID string.</verify>
  <done>PROJECTS_DS_ID confirmed and ready for use in Task 1.3.</done>
</task>

<task type="auto" id="1.3">
  <name>Task 1.3: Create Tasks Notion Database</name>
  <files>.planning/state.json (written in Task 1.4)</files>

  <action>
Call `notion-notion-create-database` to create the Tasks database.

**CRITICAL:** This call MUST come after Task 1.2. PROJECTS_DS_ID must be known.

**Status select options MUST exactly match these strings** (canonical values from
`src/normalizeStatus.mjs` — any deviation will break Phase 3 sync):
- `"new"` (gray)
- `"in progress"` (blue)
- `"done"` (green)
- `"pending"` (yellow)

**If tool takes JSON:**
```json
{
  "parent": {
    "type": "page_id",
    "page_id": "<PARENT_PAGE_ID from Task 0.4>"
  },
  "title": [
    { "type": "text", "text": { "content": "Tasks" } }
  ],
  "properties": {
    "Name": { "title": {} },
    "Status": {
      "select": {
        "options": [
          { "name": "new",         "color": "gray"   },
          { "name": "in progress", "color": "blue"   },
          { "name": "done",        "color": "green"  },
          { "name": "pending",     "color": "yellow" }
        ]
      }
    },
    "Summary":    { "rich_text": {} },
    "Raw Notes":  { "rich_text": {} },
    "Date":       { "date": {} },
    "Project": {
      "relation": {
        "data_source_id": "<PROJECTS_DS_ID>",
        "type": "dual_property",
        "dual_property": {}
      }
    }
  }
}
```

**If tool takes DDL string:**
```
Name TITLE,
Status SELECT('new':gray, 'in progress':blue, 'done':green, 'pending':yellow),
Summary RICH_TEXT,
'Raw Notes' RICH_TEXT,
Date DATE,
Project RELATION('<PROJECTS_DS_ID>', DUAL)
```

**After the call:**
Capture `response.id` → store as **TASKS_DB_ID**.

**On failure:**
- 400 with relation error → PROJECTS_DS_ID is wrong. Go back to Task 1.2, try the fallback
  (use PROJECTS_DB_ID directly). Then retry Task 1.3.
- 400 with select error → Status option names contain a typo. Verify exact strings against
  the list above.
- Any other error: Log full response. Do not write state.json. Stop and report.
  </action>

  <verify>
    notion-notion-create-database returns `object: "database"` with an `id` field.
    TASKS_DB_ID is captured and non-empty.
  </verify>
  <done>
    Tasks DB created with all 6 properties. TASKS_DB_ID captured.
    Projects DB auto-received a "Tasks" back-link property (dual_property relation).
  </done>
</task>

<task type="auto" id="1.4">
  <name>Task 1.4: Write Database IDs to .planning/state.json</name>
  <files>.planning/state.json</files>

  <action>
Write `.planning/state.json` with both database UUIDs.

**Exact file content** (replace UUIDs with actual captured values):
```json
{
  "projectsDbId": "<PROJECTS_DB_ID>",
  "tasksDbId": "<TASKS_DB_ID>"
}
```

Use a file write to create/overwrite `.planning/state.json` with this exact content.

**Verification after write:**
```bash
cat .planning/state.json
```
Confirm output matches the expected structure with real UUIDs (not placeholder strings).

**Do NOT commit state.json.** It is gitignored (configured in Phase 1). If `git status`
shows it as untracked, that's expected — `.planning/state.json` must NOT be committed.

**Format rules:**
- 2-space indent
- No trailing comma
- Keys exactly: `projectsDbId`, `tasksDbId` (camelCase, no underscores)
- Values: quoted UUID strings
  </action>

  <verify>
    `cat .planning/state.json` outputs valid JSON with both keys and UUID values.
    `git status` does NOT show state.json as a tracked file change.
  </verify>
  <done>
    .planning/state.json written with projectsDbId and tasksDbId. State cache is live.
    NDB-03 satisfied.
  </done>
</task>


<!-- ═══════════════════════════════════════════════
     WAVE 2 — RELATION VALIDATION
     ═══════════════════════════════════════════════ -->

<task type="auto" id="2.1">
  <name>Task 2.1: Create Test Project Page</name>

  <action>
**Re-run guard:** If arriving from Task 0.2 (both DBs already existed and were reachable),
relation validation was already confirmed during initial setup. Log:
> "DBs already exist and were verified at initial setup — skipping test page creation."
Then skip to the Phase 2 summary. Only proceed below if this is the first-time creation path.

Create a throwaway Project page to use as the relation target for the test Task.

```
notion-notion-create-pages
  parent: <PROJECTS_DB_ID>
  properties:
    Name: "TEST-PROJECT-PHASE2-VALIDATION"
```

**JSON property format for create-pages:**
```json
{
  "parent": { "database_id": "<PROJECTS_DB_ID>" },
  "properties": {
    "Name": {
      "title": [{ "type": "text", "text": { "content": "TEST-PROJECT-PHASE2-VALIDATION" } }]
    }
  }
}
```

Capture `response.id` → store as **TEST_PROJECT_PAGE_ID**.

**On failure:**
- 403: Integration lacks access to Projects DB. Check Notion integration settings.
- 400: Property format wrong. The Projects DB may have a different title property name.
  Call `notion-notion-fetch(PROJECTS_DB_ID)` to inspect the actual property names.
  </action>

  <verify>
    notion-notion-create-pages returns `object: "page"` with `id` field.
    TEST_PROJECT_PAGE_ID is captured.
  </verify>
  <done>Test Project page created. UUID captured for relation wiring in Task 2.2.</done>
</task>

<task type="auto" id="2.2">
  <name>Task 2.2: Create Test Task Page (linked to Test Project)</name>

  <action>
Create a throwaway Task page with its `Project` relation property set to the test Project page.

**JSON format:**
```json
{
  "parent": { "database_id": "<TASKS_DB_ID>" },
  "properties": {
    "Name": {
      "title": [{ "type": "text", "text": { "content": "TEST-TASK-PHASE2-VALIDATION" } }]
    },
    "Status": {
      "select": { "name": "new" }
    },
    "Project": {
      "relation": [{ "id": "<TEST_PROJECT_PAGE_ID>" }]
    }
  }
}
```

Capture `response.id` → store as **TEST_TASK_PAGE_ID**.

**On failure (400 on Project relation property):**
This indicates the Tasks DB's relation property is broken (wrong data_source_id was used).
- Inspect the Tasks DB schema: `notion-notion-fetch(TASKS_DB_ID)`
- Look at `properties.Project.relation.data_source_id` in the response
- Compare against PROJECTS_DS_ID used in Task 1.3
- If mismatch: the DB was created with wrong ID. Must delete and re-create Tasks DB.
  Call `notion-notion-update-page(TASKS_DB_ID, { in_trash: true })` to remove it,
  then repeat Tasks 1.2 and 1.3 with the correct data_source_id.
  </action>

  <verify>
    notion-notion-create-pages returns `object: "page"` with `id` field.
    TEST_TASK_PAGE_ID is captured. No 400 error on the Project property.
  </verify>
  <done>Test Task page created with Project relation set. Proceeding to verify relation resolves.</done>
</task>

<task type="auto" id="2.3">
  <name>Task 2.3: Verify Relation Resolves (Fetch Test Task)</name>

  <action>
Fetch the test Task page and confirm the Project relation property is non-empty and points
to the correct Project page.

```
notion-notion-fetch
  id: <TEST_TASK_PAGE_ID>
```

**What to look for in the response:**

```json
{
  "properties": {
    "Project": {
      "relation": [
        { "id": "<TEST_PROJECT_PAGE_ID>" }
      ]
    }
  }
}
```

**Validation checks:**

✅ PASS conditions (ALL must be true):
1. `response.properties.Project` exists and is not null
2. `response.properties.Project.relation` is an array with at least one entry
3. `response.properties.Project.relation[0].id` equals TEST_PROJECT_PAGE_ID (case-insensitive UUID comparison)

❌ FAIL conditions:
- `response.properties.Project.relation` is empty `[]` → relation wiring failed silently
- `response.properties.Project` is absent → property name mismatch in Tasks DB schema
- `response.properties.Project.relation[0].id` does not match TEST_PROJECT_PAGE_ID → wrong page linked

**On failure:**
Log the full response.properties.Project value.
This is a blocking failure — NDB-04 cannot be satisfied without working relations.
Do not proceed to cleanup. Report the failure with full detail so the DB schemas can be
inspected and corrected.

**On pass:**
Log: "✅ Relation validation passed. Task correctly linked to Project via UUID."
Proceed to Task 2.4 (cleanup).
  </action>

  <verify>
    response.properties.Project.relation[0].id === TEST_PROJECT_PAGE_ID
    NDB-04 satisfied: relation is UUID-based, not name-based.
  </verify>
  <done>
    Relation confirmed working. A Task's Project property correctly stores and resolves
    the UUID of the linked Project page.
  </done>
</task>

<task type="auto" id="2.4">
  <name>Task 2.4: Cleanup — Trash Test Pages</name>

  <action>
Move both test pages to Notion trash. This prevents orphaned test data from appearing in
the live workspace.

**Trash the Test Task first** (dependent page first).

`in_trash` is a **top-level parameter**, not nested under `properties`:
```
notion-notion-update-page
  id: <TEST_TASK_PAGE_ID>
  in_trash: true
```

**Then trash the Test Project:**
```
notion-notion-update-page
  id: <TEST_PROJECT_PAGE_ID>
  in_trash: true
```

**Format fallback:** If the MCP tool wraps all params under `properties`, try:
```
notion-notion-update-page
  id: <TEST_TASK_PAGE_ID>
  properties: { "in_trash": true }
```

**On failure (if `in_trash` parameter is not supported by the remote MCP in either form):**
This is non-blocking. Note in the summary:
  "Test pages could not be trashed via MCP (in_trash parameter not supported).
   Manual cleanup needed:
   - Task page: <TEST_TASK_PAGE_ID>
   - Project page: <TEST_PROJECT_PAGE_ID>
   Open Notion and delete these manually."

Phase 2 is still complete even if cleanup fails — the validation in Task 2.3 already passed.
  </action>

  <verify>
    Both notion-notion-update-page calls return success, OR failure is noted for manual cleanup.
    Either way, validation from Task 2.3 is the authoritative pass/fail signal.
  </verify>
  <done>
    Test pages trashed (or flagged for manual cleanup). Workspace is clean.
    Phase 2 setup complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → Notion MCP | Claude sends tool calls over authenticated MCP session; OAuth token managed by Copilot CLI |
| .planning/state.json → Phase 3 | State file is read by Phase 3; a tampered file with wrong UUIDs would cause sync to wrong DBs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | .planning/state.json | accept | Personal tool, single-user, no network exposure; file is gitignored |
| T-02-02 | Spoofing | notion-notion-create-database | accept | OAuth managed by Copilot CLI session; no credentials handled by this plan |
| T-02-03 | Information Disclosure | state.json contains DB UUIDs | accept | UUIDs are non-sensitive identifiers; no secrets or PII stored |
| T-02-04 | Denial of Service | Stale state.json causes Phase 3 to fail | mitigate | Task 0.2 verifies DB reachability before trusting cached IDs; 404 triggers re-creation |
| T-02-05 | Elevation of Privilege | Relation wiring to wrong DB | mitigate | Task 1.2 explicitly captures data_source_id; Task 2.3 verifies UUID match before accepting setup as complete |
</threat_model>

<verification>
## Phase 2 Verification Checklist

Run these checks after all tasks complete:

### V1 — state.json exists and is valid
```bash
cat .planning/state.json
```
Expected:
```json
{
  "projectsDbId": "<uuid>",
  "tasksDbId": "<uuid>"
}
```
- [ ] File exists
- [ ] Valid JSON (no parse errors)
- [ ] Both keys present: `projectsDbId`, `tasksDbId`
- [ ] Both values are non-empty UUID strings (format: `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`)

### V2 — state.json is NOT committed to git
```bash
git status .planning/state.json
```
Expected: file is either "not tracked" or listed under `.gitignore`.
- [ ] state.json does not appear as a staged or modified tracked file

### V3 — Projects DB reachable and has correct schema
Call: `notion-notion-fetch(projectsDbId)`
- [ ] Returns `object: "database"`
- [ ] `properties.Name` has type `title`
- [ ] `properties.Description` has type `rich_text`

### V4 — Tasks DB reachable and has correct schema
Call: `notion-notion-fetch(tasksDbId)`
- [ ] Returns `object: "database"`
- [ ] `properties.Name` has type `title`
- [ ] `properties.Status` has type `select` with exactly 4 options: `new`, `in progress`, `done`, `pending`
- [ ] `properties.Summary` has type `rich_text`
- [ ] `properties["Raw Notes"]` has type `rich_text`
- [ ] `properties.Date` has type `date`
- [ ] `properties.Project` has type `relation`
- [ ] `properties.Project.relation.data_source_id` matches the Projects DB data source UUID

### V5 — Relation validation passed (from Task 2.3)
- [ ] Test Task's Project relation resolved to correct Project page UUID
- [ ] No 400 error on relation property during test page creation

### V6 — Idempotency (re-run safety)
Conceptual check — if Tasks 0.1 and 0.2 run again with the current state.json:
- [ ] Both IDs would be found in state.json
- [ ] Both DBs would be confirmed reachable
- [ ] Plan would log "DBs already exist" and skip to validation
- [ ] No duplicate databases created
</verification>

<success_criteria>
Phase 2 is complete when ALL of the following are TRUE:

1. **Projects DB exists** in Notion with exactly: Name (title), Description (rich_text)
2. **Tasks DB exists** in Notion with exactly: Name (title), Status (select with `new`/`in progress`/`done`/`pending`), Summary (rich_text), Raw Notes (rich_text), Date (date), Project (relation → Projects DB, dual_property)
3. **`.planning/state.json` exists** with valid JSON containing `projectsDbId` and `tasksDbId` as UUID strings
4. **Relation is UUID-based**: A Task page's Project relation resolves to the correct Project page UUID (not a name string)
5. **Idempotent**: Re-running this plan when state.json is populated and DBs are live results in "DBs already exist" log and no new databases created
6. **No test data orphaned**: Test pages are trashed (or flagged for manual cleanup if MCP doesn't support in_trash)

Requirements satisfied: NDB-01, NDB-02, NDB-03, NDB-04
</success_criteria>

<output>
After all tasks complete and verification passes, create:
`.planning/phases/02-notion-database-setup/02-01-SUMMARY.md`

Include:
- Which path was taken (fresh creation vs. idempotency skip)
- The parent page decision and outcome (which Notion page hosts the DBs)
- PROJECTS_DB_ID and PROJECTS_DS_ID values (for Phase 3 reference)
- TASKS_DB_ID value
- Whether `data_sources[0].id` was in create-database response or required a fetch call
- Which assumption from RESEARCH.md was confirmed or refuted:
  - A1: Tool format (JSON vs DDL)
  - A2: `data_sources` presence in create-database response
  - A4: `in_trash` parameter support
- Any failures encountered and how they were resolved
- Relation validation result (pass/fail + evidence)
- Cleanup status (trashed via MCP / manual cleanup needed)
</output>
